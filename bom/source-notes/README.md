# Source Notes

`mybom_raw_links.txt` is the original rough purchase list from the build. It is kept here as a raw reference so the cleaned BOM files can stay readable without losing the original notes.
